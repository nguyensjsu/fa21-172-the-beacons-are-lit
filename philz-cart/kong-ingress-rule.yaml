apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: philz-api
  annotations:
    kubernetes.io/ingress.class: kong
spec:
  rules:
  - http:
      paths:
      - path: /rest
        backend:
          serviceName: philz-api-service
          servicePort: 80
